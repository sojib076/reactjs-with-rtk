import Loading from "@/components/CommonComponents/Loading";

const Dashboard = () => {
    return (
        <div className="mt-">
            <Loading />
        </div>
    );
};

export default Dashboard;